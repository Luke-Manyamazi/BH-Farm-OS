import { FormEvent, useEffect, useState } from 'react';
import { Redirect } from 'wouter';
import { useAuth } from '@/lib/auth';
const API=import.meta.env.VITE_API_URL||'';async function api(path:string,options?:RequestInit){const r=await fetch(`${API}${path}`,{...options,credentials:'include',headers:{'Content-Type':'application/json',...(options?.headers||{})}});const d=await r.json();if(!r.ok)throw new Error(d.message||'Request failed');return d}
export default function UsersPage(){const{user}=useAuth();const[users,setUsers]=useState<any[]>([]);const[roles,setRoles]=useState<any[]>([]);const[sections,setSections]=useState<any[]>([]);const[name,setName]=useState('');const[email,setEmail]=useState('');const[password,setPassword]=useState('');const[role,setRole]=useState('field_worker');const[selected,setSelected]=useState<string[]>([]);const[message,setMessage]=useState('');useEffect(()=>{if(user?.permissions.includes('users.manage'))Promise.all([api('/api/auth/users'),api('/api/auth/roles'),api('/api/auth/sections')]).then(([u,r,s])=>{setUsers(u);setRoles(r);setSections(s)}).catch(e=>setMessage(e.message))},[user]);if(!user?.permissions.includes('users.manage'))return <Redirect to="/"/>;async function submit(e:FormEvent){e.preventDefault();setMessage('');try{await api('/api/auth/users',{method:'POST',body:JSON.stringify({displayName:name,email,password,role,sections:selected})});setMessage('User created.');setName('');setEmail('');setPassword('');setUsers(await api('/api/auth/users'))}catch(e){setMessage(e instanceof Error?e.message:'Unable to create user')}}return <div className="space-y-6"><div><div className="font-mono text-[10px] uppercase tracking-[.2em] text-[hsl(var(--primary))]">Administration</div><h1 className="mt-2 text-3xl font-bold">Users & access</h1><p className="mt-2 text-sm text-[hsl(var(--muted-foreground))]">Create users, assign roles and limit section access.</p></div><div className="grid gap-6 lg:grid-cols-[1fr_1.3fr]"><form onSubmit={submit} className="rounded-2xl border p-5 space-y-4"><h2 className="font-semibold">Create user</h2><input className="w-full rounded-xl border p-3" placeholder="Full name" value={name} onChange={e=>setName(e.target.value)} required/><input className="w-full rounded-xl border p-3" placeholder="Email" type="email" value={email} onChange={e=>setEmail(e.target.value)} required/><input className="w-full rounded-xl border p-3" placeholder="Temporary password (12+ characters)" type="password" value={password} onChange={e=>setPassword(e.target.value)} minLength={12} required/><select className="w-full rounded-xl border p-3" value={role} onChange={e=>setRole(e.target.value)}>{roles.map(r=><option key={r.key} value={r.key}>{r.name}</option>)}</select><div><div className="mb-2 text-sm font-medium">Assigned sections</div><div className="grid grid-cols-2 gap-2">{sections.map(s=><label key={s.key} className="flex gap-2 text-sm"><input type="checkbox" checked={selected.includes(s.key)} onChange={e=>setSelected(v=>e.target.checked?[...v,s.key]:v.filter(x=>x!==s.key))}/>{s.name}</label>)}</div></div>{message&&<div className="text-sm">{message}</div>}<button className="w-full rounded-xl bg-[hsl(var(--primary))] p-3 font-semibold text-[hsl(var(--primary-foreground))]">Create user</button></form><div className="rounded-2xl border p-5"><h2 className="mb-4 font-semibold">Current users</h2><div className="space-y-2">{users.map(u=><div key={u.id} className="flex items-center justify-between rounded-xl border p-3"><div><div className="font-medium">{u.displayName}</div><div className="text-xs text-[hsl(var(--muted-foreground))]">{u.email}</div></div><div className="text-right text-xs"><div className="font-semibold">{u.roleName}</div><div>{u.active?'Active':'Disabled'}</div></div></div>)}</div></div></div></div>}
